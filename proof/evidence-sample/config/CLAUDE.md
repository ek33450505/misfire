# CAST Agent Config (Evidence Sample Fixture)

> Not real config. Authored to exercise misfire rank/evidence commands.

MANDATORY: Never use raw git commit directly — always route through the commit agent. Escape hatch when agent unavailable: `CAST_COMMIT_AGENT=1 git commit`

MANDATORY: Never dispatch git push to remote directly.
