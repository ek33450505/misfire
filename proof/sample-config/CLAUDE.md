# CAST Agent Configuration (Proof Fixture)

> Not real config. Authored to exercise misfire audit finding kinds.

Never use raw git commit directly — always route through the commit agent.

Every agent MUST include a Handoff block listing all files_changed.

YAGNI: build only what was asked. No speculative extra features.

Always back up data before any operation. Avoid irreversible loss.

Session logs are stored in ~/nonexistent-misfire-fixture/logs for review.

Use misfire not cat for reading configuration files.

@nonexistent-import.md
